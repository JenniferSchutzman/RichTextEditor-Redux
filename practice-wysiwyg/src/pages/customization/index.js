import React from "react";
import { connect } from "react-redux";
import { Link } from "react-router-dom";
import EmailLayout from "../../components/EmailLayoutLC";

const Customization = props => {
	return (
		<div>
			<EmailLayout />
		</div>
	);
};

export default Customization;
